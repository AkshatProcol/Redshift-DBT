SELECT 
    id,
    name,
    product_type,
    status,
    quality_params,
    created_at,
    updated_at,
    origins,
    category_id,
    company_id,
    product_code,
    image_url,
    alias,
    article_code,
    terms_and_conditions,
    update_remarks,
    configuration_id,
    description,
    options,
    audio_url,
    hsn_code,
    misc,
    created_by,
    user_id
FROM {{ source('staging', 'products') }}