SELECT 
    id,
    trade_request_id,
    product_id,
    product_quality_id,
    quantity,
    status,
    buyer_hub_id,
    product_origins,
    remaining_quantity,
    auction_config,
    unit_type,
    current_status,
    created_at,
    updated_at,
    product_options,
    price_ceiling,
    price_floor,
    other_details,
    price,
    gst,
    floor_price,
    config,
    ceil_price,
    floor_quantity,
    validations,
    projected_price_range,
    meta_data,
    product_options_identifier,
    template_data,
    sheet_order,
    rank_hash,
    created_by_user_id,
    variant_details,
    trade_identifiers,
    event_score_for_participants,
    unit_id,
    section,
    "order" as order_no,
    event_score_for_buyers,
    rank_one_price,
    rank_one_total_landed_amount,
    auction_analytics_json
FROM {{ source('staging', 'trade_products') }}