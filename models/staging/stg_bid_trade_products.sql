SELECT 
    id,
    trade_product_id,
    status,
    price,
    quantity,
    quality_params,
    images,
    remarks,
    product_sample_id,
    city_id,
    applicable_apmc_product_config_id,
    rank_price,
    final_price,
    gst,
    ancestry,
    price_breakup_json,
    created_at,
    updated_at,
    other_details,
    meta_data,
    template_data,
    company_id,
    score
FROM {{ source('staging', 'bid_trade_products') }}