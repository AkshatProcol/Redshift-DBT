SELECT 
    id,
    user_id,
    trade_request_id,
    status,
    delivery_type,
    company_id,
    product_sample_id,
    city_id,
    broker_id,
    broker_company_id,
    applicable_apmc_product_config_id,
    contract_id,
    session_id,
    approval_status,
    lock_version,
    remarks,
    uuid,
    ip_info,
    created_at,
    updated_at,
    end_time,
    start_time,
    closed_at,
    placed_at,
    negotiable,
    is_multi_product,
    price,
    quantity,
    gst,
    rank_price,
    final_price,
    amount,
    score,
    quality_params,
    terms_and_conditions,
    trade_credit_discount,
    images,
    other_details,
    bid_product_ids,
    price_breakup_json,
    template_data,
    meta_data,
    context
FROM {{ source('staging', 'bids') }}